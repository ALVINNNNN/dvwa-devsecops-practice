# Minimizes the "Server" response header (Server Leaks Version Information).
# Apache doesn't support fully blanking this header without a custom module,
# so this reduces it from e.g. "Apache/2.4.25 (Debian)" down to just "Apache".
ServerTokens Prod
ServerSignature Off

# Set at the web-server layer (not in PHP) so these cover EVERY response,
# including static assets (css/images/robots.txt) that never run DVWA's PHP
# code and so never saw the equivalent header() calls there.
Header always set X-Content-Type-Options "nosniff"
Header always set X-Frame-Options "SAMEORIGIN"
Header always set Permissions-Policy "geolocation=(), microphone=(), camera=()"
Header always set Cross-Origin-Opener-Policy "same-origin"
Header always set Cross-Origin-Resource-Policy "same-origin"

# config/config.inc.php.dist is a setup template (copied to config.inc.php
# during install/build), never included by any PHP code. Because of its
# .dist extension Apache doesn't execute it as PHP, so requesting it
# directly over HTTP would return the raw PHP source (and its example
# default credentials) instead of running it. Block direct access to any
# stray .dist template files instead.
<FilesMatch "\.dist$">
	Require all denied
</FilesMatch>
